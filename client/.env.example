# Copy to client/.env and replace with your real domain. After HTTPS is up,
# ALWAYS use https / wss — the server's port 8000 is no longer exposed
# publicly, all traffic goes through Caddy on 443.
VITE_API_URL=https://yourname.duckdns.org
VITE_WS_URL=wss://yourname.duckdns.org
