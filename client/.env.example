# Copy to client/.env. Use https / wss — the server sits behind nginx with a
# Let's Encrypt cert; the raw HTTP port 8000 is bound to localhost only and
# not reachable from outside.
VITE_API_URL=https://2-26-117-77.sslip.io
VITE_WS_URL=wss://2-26-117-77.sslip.io
